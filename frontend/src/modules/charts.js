/**
 * @typedef {Object} PolarChartGraphics
 * @property {number} length
 * @property {string} label
 * @property {[]} datasets
 * @property {string} key
 */


export const createPolarChartGraphics = ({ size, label, data }) => {
  return {
    datasets: [
      
    ]
  }
}