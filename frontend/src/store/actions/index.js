/**
 * @typedef {string} HTTPBodyKey
 */

/**
 * All data is stored on "response" on every call on the Rest API.
 * Once you get data should contain a "response" key.
 * If the convention changes, we simply change this string.
 */
const response = 'response'

export default response